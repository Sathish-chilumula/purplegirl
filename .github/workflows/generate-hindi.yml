name: Translate + Rewrite (Legacy — Disabled)
# This workflow has been REPLACED by two separate dedicated workflows:
#   - rewrite-articles.yml  (runs every 4 hours)
#   - translate-articles.yml (runs every 4 hours, offset by 2h)
# Keeping this file to avoid GitHub workflow deletion errors on old run references.

on:
  workflow_dispatch:  # Manual trigger only — not on schedule anymore

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is disabled
        run: echo "Use rewrite-articles.yml and translate-articles.yml instead."
